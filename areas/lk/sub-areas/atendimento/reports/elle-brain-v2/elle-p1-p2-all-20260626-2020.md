# Elle — P1/P2 execução completa pós-auditoria

**Data:** 2026-06-26 20:20 UTC  
**Pedido:** Lucas pediu “fazer P1 e P2 todos”.  
**Escopo:** P1 env/recreate canonical, P1 qualidade/eval_bad, P2 conversion attribution, P2 tag noise, P2 crons stale.  
**values_printed:** false

## Resumo executivo

Executado:

1. P1 env canonical seguro via Doppler/manifest/compose/script.
2. P1 `eval_bad`/risco convertido em candidates supervisionados.
3. P2 conversion monitor agora atribui por origem operacional (`decision_source`, v2, handoff).
4. P2 tag noise corrigido no observer.
5. P2 crons stale desativados arquivados.
6. Verificações finais rodadas.

**Ressalva:** compose/env canonical está preparado, mas o CLI de compose (`docker compose` plugin ou `docker-compose`) não está disponível neste shell. O script de recreate agora falha de forma segura com `compose_cli_missing` em vez de recriar sem controle. Não instalei pacote no VPS nesta rodada para não alterar sistema operacional sem etapa específica.

## P1 — env/recreate canonical seguro

Arquivos criados/alterados:

- `/opt/elle-chatwoot/docker-compose.yml` agora declara `environment:` para variáveis Elle/Chatwoot/OpenRouter.
- `/opt/elle-chatwoot/env.manifest.json` documenta quais envs são secret vs defaults.
- `/opt/elle-chatwoot/scripts/recreate_elle_doppler_safe.sh` roda recreate via Doppler sem gravar secrets em `.env`.

Secrets verificados por nome, sem valores:

```text
OK CHATWOOT_BASE_URL
OK CHATWOOT_ACCOUNT_ID
OK CHATWOOT_LK_API_TOKEN
OK ELLE_CHATWOOT_WEBHOOK_SECRET
OK OPENROUTER_API_KEY
```

Manifest:

```json
{
  "compose_cli_available": false,
  "do_not_store_values_in_env_file": true,
  "values_printed": false
}
```

Interpretação: env canonical está mapeado e seguro, mas recreate automático exige disponibilizar compose CLI antes de usar.

## P1 — qualidade / eval_bad / regression candidates

Criado:

- `/opt/elle-chatwoot/scripts/export_eval_risk_candidates.py`
- `/app/scripts/export_eval_risk_candidates.py`

Resultado:

```json
{
  "status": "ok",
  "eval_risk_seen": 4,
  "candidates_added": 3,
  "candidates_total": 3,
  "customer_send_executed": false,
  "writes_external": 0,
  "values_printed": false
}
```

Learner agora integra esses candidates:

```json
{
  "status": "ok",
  "v2_lessons_loaded": 8,
  "regression_candidates_total": 14,
  "eval_risk_seen": 4,
  "eval_risk_candidates_total": 3,
  "customer_send_executed": false,
  "writes_external": 0,
  "values_printed": false
}
```

## P2 — conversion monitor com atribuição por origem

Alterado:

- `/opt/data/profiles/lk-ops/scripts/lk_elle_conversion_monitor.py`

Agora separa por:

- `elle_brain_v2_canary`
- `llm_final`
- `rule_guardrail_after_llm`
- `handoff_or_stock`
- `other_or_observed`

Smoke read-only:

```text
conversas elegíveis com Elle: 37
conversas com venda atribuída: 1
receita atribuída aproximada: R$ 2.399,99

Atribuição por origem operacional:
- handoff_or_stock: conversas=18, vendas=1, receita≈R$ 2.399,99
- other_or_observed: conversas=7, vendas=0
- llm_final: conversas=5, vendas=0
- elle_brain_v2_canary: conversas=4, vendas=0
- rule_guardrail_after_llm: conversas=3, vendas=0
```

## P2 — tag noise corrigido

Problema: tags string eram iteradas como caracteres (`a`, `o`, `e`, `_`).

Patch no remote observer:

- `/opt/elle-chatwoot/elle_observer_summary.py`

Resultado:

```text
- tags: human_takeover_event: 104, incoming_blocked_by_existing_human_assignee: 74, incoming_processed: 47, event_filter: 27, produto: 24, prazo: 21, pos_venda: 19, followup_pos_handoff: 13
```

## P2 — crons stale arquivados

Arquivados em:

```text
/opt/data/profiles/lk-ops/cron/archive/elle-stale-disabled-jobs-20260626.json
```

Jobs removidos de `jobs.json`:

- `74c828f4331b` — semanal sábado 17h00 desativado;
- `lk-elle-daily-sunday-trial` — domingo teste 1 semana;
- `lk-elle-daily-trial-review-20260621` — revisão trial expirada.

Jobs Elle restantes:

```json
[
  {"id": "3f044d9c6f99", "enabled": true, "last_status": "ok"},
  {"id": "45a6cd07c138", "enabled": true, "last_status": "ok"},
  {"id": "ee692193a666", "enabled": true, "last_status": "ok"},
  {"id": "lk-elle-customer-correction-learner", "enabled": true, "last_status": "ok"}
]
```

## Verificações finais

Runtime:

```json
{
  "ok": true,
  "dry_run": false,
  "write_enabled": true,
  "kill_switch": false,
  "public_reply_enabled": true,
  "ai_enabled": true,
  "ai_provider": "openrouter",
  "ai_model": "deepseek/deepseek-v4-pro",
  "elle_brain_v2_canary_enabled": true,
  "elle_brain_v2_canary_percent": 100,
  "values_printed": false
}
```

Drift/testes:

```text
check_drift.sh: OK
regression suite: 38 OK
```

OpenRouter:

```text
✅ Elle OpenRouter 1h: OK
processed=12 | openrouter=12 | v2_used=4 | v2_skipped=8 | v2_errors=0 | provider_errors=0
```

Autonomy gate:

```json
{
  "status": "hold",
  "processed": 47,
  "v2_canary_used": 4,
  "v2_canary_skipped": 9,
  "v2_canary_error": 0,
  "ai_provider_error": 0,
  "eval_bad": 4,
  "eval_medium_high": 1,
  "handoff_violations": 0,
  "recommendation": "do_not_expand_autonomy"
}
```

## Estado após P1/P2

| Item | Status |
|---|---|
| Runtime | OK |
| v2 100% safe-only | OK |
| Drift | OK |
| Tests | OK |
| Learner | OK |
| Eval risk candidates | OK |
| Conversion attribution | OK |
| Tag noise | OK |
| Crons stale | Arquivados |
| Env canonical | Preparado, secrets OK |
| Compose CLI para recreate | **Bloqueado/ausente** |
| Autonomia ampla | **HOLD** |

## Próximo bloqueio real

Para chamar a plataforma de 100% operacional/recriável, ainda falta uma decisão específica:

- instalar/disponibilizar `docker compose` plugin ou `docker-compose` no ambiente;
- rodar `recreate_elle_doppler_safe.sh` em janela controlada;
- validar health/drift/logs pós-recreate.

Não executei essa instalação/recreate nesta rodada para evitar mexer no sistema Docker/host sem etapa dedicada.

## Backup

```text
/opt/data/backups/elle-p1-p2-all/20260626T201347Z
```
