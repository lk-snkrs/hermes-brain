# MAPA — LC Claude CLI

Subárea para artefatos do profile `lc-claude-cli`: broker/auth/CLI, backups, runtime validation e receipts.

Guardrail: não copiar secrets, caches OAuth ou tokens entre profiles; usar broker/Doppler-first e reportar apenas status, values_printed=false.
