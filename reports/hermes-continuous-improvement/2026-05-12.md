# Hermes/LK Continuous Improvement — 2026-05-12 02:00 BRT

Contexto: Infra/Hermes + LK/Hermes Brain.  
Ação: read-only + documentação operacional.  
Risco: baixo; nenhuma mudança em Docker, gateway, containers, secrets, bancos, Shopify, Meta/Google, campanhas ou envios externos.

## Verificações read-only executadas

- Data/hora do run: `2026-05-12T05:00:56+00:00`.
- Runtime local no container: `/opt/hermes/.venv/bin/hermes --version` retornou `Hermes Agent v0.13.0 (2026.5.7)`.
- Config ativo: `/opt/data/config.yaml`.
- `hermes config check`: config version `23` OK.
- `hermes cron list`: 8 jobs ativos, incluindo:
  - `f5a23dd6a1bd` — Hermes/LK daily continuous improvement scan;
  - `edd06fe19397` — Hermes runtime + cron watchdog no_agent;
  - `e7a61e275c37` — Hermes v0.13 artifacts freshness watchdog no_agent;
  - `7c688553e293` — LK Daily Sales Brief read-only mandatory delivery;
  - `953b9055458e` — LK Weekly CEO Review read-only mandatory delivery;
  - `d4c26da4cd48` — LK GMC Review read-only mandatory delivery.
- Watchdogs `no_agent` testados manualmente:
  - `hermes_runtime_cron_watchdog.py`: `rc=0`, stdout vazio, stderr vazio.
  - `hermes_artifacts_freshness_watchdog.py`: `rc=0`, stdout vazio, stderr vazio.
- Kanban `lk-growth-ops`: 8 cards concluídos e 5 cards `ready` sem assignee; nenhum worker/daemon novo ativado.
- Logs locais recentes:
  - gateway Telegram enviou respostas normalmente na madrugada;
  - erro recente relevante foi timeout de edição de mensagem Telegram em 2026-05-11 20:03 UTC, com fallback de rede; não há evidência de falha persistente de startup;
  - compressões tiveram fallback para OpenRouter/Gemini após erro de conexão no auxiliar, sem fallback destructivo de contexto observado neste recorte.

## Release check

Fonte: GitHub releases API de `NousResearch/hermes-agent`.

Última release pública encontrada: `v2026.5.7` — `Hermes Agent v0.13.0 (2026.5.7) — The Tenacity Release`, publicada em 2026-05-07.

Conclusão: não há release pública posterior à versão ativa esperada. Não há plano de update de runtime hoje.

## Diferenças/gaps observados

1. **Sem acesso ao Docker socket neste contexto**  
   `docker ps`, `docker exec` e `docker logs` falharam com `Cannot connect to the Docker daemon at unix:///var/run/docker.sock`. Portanto, este run não confirmou as imagens dos containers por Docker nem leu logs Docker diretamente. Isso não prova falha de produção; apenas confirma a lacuna de observabilidade do contexto do cron.

2. **Detector de gateway inconsistente neste run**  
   `hermes cron status`/`hermes status --all` reportou gateway como parado, mas o próprio cron está executando e os logs locais mostram atividade recente do gateway Telegram. Tratar como discrepância de detector/ambiente até inspeção Hostinger/Docker read-only confirmar o contrário. Não executar `hermes gateway install`, restart ou systemd host como remediação automática.

3. **`hermes status --all` pode mostrar fragmentos/valores de chaves em stdout**  
   O comando é útil para diagnóstico interno, mas o output bruto não deve ser colado em relatórios ao Lucas nem no Brain sem sanitização.

## Implementado hoje

- Criado este relatório durável em `reports/hermes-continuous-improvement/2026-05-12.md`.
- Atualizada a rotina `areas/operacoes/rotinas/hermes-v013-operacionalizacao.md` com o resumo do run diário de 2026-05-12.
- Atualizada a skill `lucas-hermes-continuous-improvement` com o pitfall de redigir/sanitizar outputs de `hermes status --all` antes de reportar.

## Classificação das oportunidades

### Adopt now

- Manter os watchdogs `no_agent` atuais. Eles continuam silenciosos em OK e cobrem runtime/cron + freshness de artefatos.
- Usar o Kanban `lk-growth-ops` como Mission Control, mas sem workers automáticos por padrão.
- Continuar registrando runs diários em Brain quando houver achado operacional; se nada novo aparecer, o cron pode responder `[SILENT]`.

### Teach Lucas manual usage

- `/goal`: usar para missões longas que não devem depender de “seguir” repetido. Exemplo: `/goal Tocar LK OS por 7 dias mantendo Mission Control, relatórios obrigatórios e aprovações separadas de writes/envios`.
- Kanban: usar para status/pendência real da operação LK, não como lista solta no chat. Falha típica: card sem fonte, sem next step ou misturando aprovação com execução.
- `/background`: usar para auditoria/pesquisa longa enquanto a conversa principal continua; não usar para Docker, deploy, secrets, produção, Shopify writes ou envios externos.

### Approval needed

- Fechar observabilidade Docker do cron: benefício é confirmar imagens/logs/containers dentro do protocolo diário; risco é tocar host/Docker/socket/SSH. Recomendação: aprovar um plano read-only com uma destas opções: SSH host restrito para inventário, socket Docker read-only controlado, ou script no host que publique apenas status sanitizado em `/opt/data/`.
- Dashboard público/local exposto: não executar sem plano de acesso, hardening e rollback.
- Qualquer troca de imagem/runtime/restart/gateway/compose: não há motivo hoje; se surgir release posterior à v0.13, preparar plano e pedir aprovação.

## Próximo passo recomendado

Prioridade: resolver a lacuna de observabilidade Docker **sem mexer em runtime**. Preparar um plano read-only para o host publicar status sanitizado dos containers esperados e últimas linhas de logs sem secrets, com aprovação Lucas antes de qualquer alteração em Hostinger/Docker/socket.
