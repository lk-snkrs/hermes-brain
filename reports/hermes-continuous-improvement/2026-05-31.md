# Daily Intelligence Loop — 2026-05-31

> Execução local/read-only com um auto-fix A1 em script local. Sem external writes, sem Docker/VPS/gateway restart, sem alteração de cron registry.

## 1. Veredito executivo

**Parcial, com alerta acionável.** O runtime principal está vivo, mas o watchdog de runtime detectou um gateway inesperado: `/opt/data/profiles/lc-claude-cli`.

O achado é importante porque o próprio receipt de 30/05 dizia que `[LC] Claude Cli` era um profile local/CLI de brainstorm, com Telegram/gateway pendente de aprovação. Hoje, o processo está vivo como `hermes gateway run`, então há drift entre documentação e runtime.

Auto-fix seguro aplicado: removi `lc-claude-cli` da lista `EXPECTED` do watchdog global `/opt/data/scripts/hermes_all_gateway_watchdog.py`, para ele não auto-iniciar esse profile novamente. **Não parei o processo já vivo**, porque parar/reiniciar gateway é mutação runtime e deve ser escopo aprovado.

## 2. Saúde runtime / observabilidade

- Host observability salvo em `reports/hermes-host-docker-observability-2026-05-31.json`.
- Containers esperados: `hermes-agent-5ajw-hermes-agent-1` e `hermes-agent-5ajw-hermes-telegram-1` em `running`.
- Runtime reportado nos containers: `Hermes Agent v0.15.1 (2026.5.29)`.
- Alertas do helper: versão esperada não confirmada nos dois containers. Classificação: **expectativa do helper stale**, não queda runtime, porque o runtime vivo é v0.15.1 e o próprio contexto atual já reconhece v0.15.1 como produção.
- Cron scheduler vivo: 23 jobs ativos; 27 jobs no total incluindo pausados.
- Watchdog de compressão `4bb4e2223fd3`: último run silencioso/OK.
- Watchdog runtime `edd06fe19397`: OK com stdout de alerta, detectando `lc-claude-cli` inesperado.
- Watchdog global gateways `b78ae7ac81d0`: OK; após patch manual rodou com rc=0/stdout vazio.

## 3. Release Hermes

Fonte: `https://api.github.com/repos/NousResearch/hermes-agent/releases?per_page=10`.

Últimas releases:

- `v2026.5.29.2` — Hermes v0.15.2 — 2026-05-29.
- `v2026.5.29` — Hermes v0.15.1 — 2026-05-29.
- `v2026.5.28` — Hermes v0.15.0 — 2026-05-28.

Classificação: runtime atual já está em v0.15.1; v0.15.2 segue como micro-oportunidade/packaging fix, **não incidente** e **não autorização de upgrade automático**.

## 4. Prioridade ativa

Foco primário: **Hermes Geral / runtime governance**.

Evidências:

1. Alerta real do watchdog runtime sobre `lc-claude-cli` como HERMES_HOME inesperado.
2. Fechamento 23h criou/documentou `[LC] Claude Cli` como profile local, mas runtime vivo contradiz o status “gateway parado”.
3. A camada de watchdog global foi alterada ontem; hoje apareceu o primeiro drift de escopo dela.
4. Brain Health segue limpo, então a prioridade não é documentação geral; é reconciliar runtime esperado vs real.

Modos por área:

- LK: manutenção; crons comerciais OK, `LK Weekly Collection Sort Rule B` segue com timeout anterior de 29/05.
- Mission Control/Mesa: manutenção; relatório 02h30 e Mesa COO continuam como visibilidade intencional.
- Zipper: manutenção/read-only; sem nova decisão crítica detectada neste run.
- SPITI: monitoramento/silêncio correto.
- Mordomo: manutenção; sem novo bloqueio lido neste run.
- Hermes/Infra: foco P0 do dia por drift de gateway esperado.

Risco de dispersão: médio. Há muito ruído de artefatos LK/receipts no working tree, mas o único sinal acionável novo é o gateway `lc-claude-cli` inesperado.

Próxima ação segura: Lucas aprovar se deseja **parar o gateway `lc-claude-cli` agora** e manter o profile apenas CLI, ou promover oficialmente o profile a Telegram com token/canal/watchdog documentados.

## 5. Segundo Cérebro / crons

Resumo de cron vivo:

- Total: 27 jobs listados; 23 ativos e 4 pausados.
- Críticos OK: daily intelligence, Fechamento 23h, relatório 02h30, runtime watchdog, compression self-heal, watchdog global gateways.
- Pausados intencionais: watchdogs fragmentados de Mordomo, LK Growth, SPITI e LK specialist, substituídos pelo watchdog global.
- Falha persistente: `LK Weekly Collection Sort Rule B` timeout 120s em 29/05; próximo run 05/06.

## 6. Brain Health / Improvement Score

- `reports/brain-health-check-2026-05-31-02h.json`: FAIL=0 / WARN=0.
- `reports/brain-improvement-score-2026-05-31-02h.md` e `.json`: score 94/100.

## 7. Melhorias aplicadas

- Patch em `/opt/data/scripts/hermes_all_gateway_watchdog.py`: removido `lc-claude-cli` de `EXPECTED` e adicionado comentário explícito de que esse profile é local/CLI, não gateway Telegram esperado sem aprovação.
- Verificação: `python3 -m py_compile /opt/data/scripts/hermes_all_gateway_watchdog.py` OK.
- Verificação: execução manual do watchdog global OK/silent.
- Learning ledger criado em `reports/hermes-learning-ledger/2026-05-31.md`.
- Brain Sync safe dry-run executado: allowlist aceitou 5 arquivos, incluindo relatório diário, learning ledger e health JSON; score `.md/.json` segue fora da allowlist padrão como gap já conhecido, sem usar `git add .`.

## 8. Decisão / aprovação necessária

**Decisão 1/1 — gateway `[LC] Claude Cli` inesperado**

Recomendação: **parar o gateway `lc-claude-cli` e manter o profile como CLI/local até Lucas aprovar ativação Telegram**.

Motivo: o receipt canônico diz “gateway parado intencionalmente”; o runtime atual tem um processo vivo em `/opt/data/profiles/lc-claude-cli`; e o profile não deveria ser auto-gerenciado pelo watchdog global.

Escopo se aprovado:

- parar somente o processo `hermes gateway run` cujo `HERMES_HOME=/opt/data/profiles/lc-claude-cli`;
- verificar que Main, Mordomo, LK Growth, SPITI, LK Ops, LK Shopify e LK Trends continuam vivos;
- rodar `hermes_runtime_cron_watchdog.py` e confirmar stdout sem `lc-claude-cli`;
- registrar receipt sanitizado no Brain.

Exclusões: sem Docker, sem VPS, sem Main gateway, sem Traefik, sem cron registry, sem secrets, sem WhatsApp/e-mail/Shopify/banco/sistemas externos.

Rollback: reiniciar somente o gateway do profile `lc-claude-cli` com API/webhook off, caso Lucas decida promover o profile a Telegram.

## 9. Fontes

- `reports/hermes-host-docker-observability-2026-05-31.json`
- `HERMES_HOME=/opt/data /opt/hermes/.venv/bin/hermes cron list --all`
- `/opt/data/scripts/hermes_runtime_cron_watchdog.py`
- `/opt/data/scripts/hermes_all_gateway_watchdog.py`
- `reports/governance/lc-claude-cli-criado-2026-05-30.md`
- `reports/governance/hermes-all-telegram-gateways-watchdog-2026-05-30.md`
- `reports/brain-health-check-2026-05-31-02h.json`
- `reports/brain-improvement-score-2026-05-31-02h.md`
- GitHub Releases API
