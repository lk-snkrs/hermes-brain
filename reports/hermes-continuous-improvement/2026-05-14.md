# Hermes/LK Continuous Improvement — 2026-05-14 02:00 BRT

Contexto: Infra/Hermes + LK/Hermes Brain.  
Ação: read-only + documentação operacional.  
Risco: baixo; nenhuma mudança em Docker, gateway, containers, secrets, bancos, Shopify, Meta/Google, campanhas ou envios externos.

## Verificações read-only executadas

- Data/hora do run: `2026-05-14T05:00Z`.
- Helper aprovado de observabilidade Hostinger/Docker executado primeiro: `/opt/data/scripts/hermes_host_docker_observability.py --output /opt/data/hermes_bruno_ingest/hermes-brain/reports/hermes-host-docker-observability-2026-05-14.json`.
- Resultado do helper: sucesso em modo read-only; JSON sanitizado salvo em `reports/hermes-host-docker-observability-2026-05-14.json`.
- Containers confirmados no host:
  - `hermes-agent-5ajw-hermes-agent-1`: imagem `hermes-agent-custom:v0.13.0-20260510`, estado `running`, status `Up 3 days`;
  - `hermes-agent-5ajw-hermes-telegram-1`: imagem `hermes-agent-custom:v0.13.0-20260510`, estado `running`, status `Up 32 hours`.
- Versões confirmadas nos dois containers:
  - `Hermes Agent v0.13.0 (2026.5.7)`;
  - Project: `/opt/hermes`;
  - Python: `3.13.5`;
  - OpenAI SDK: `2.33.0`.
- Fallback/local no container também conferido:
  - `/opt/hermes/.venv/bin/hermes --version` retornou `Hermes Agent v0.13.0 (2026.5.7)`;
  - config ativo: `/opt/data/config.yaml`;
  - `hermes config check`: config version `23` OK;
  - `hermes cron status`: gateway/scheduler rodando, PID `7`, 8 jobs ativos, próximo run `2026-05-14T05:30:00+00:00`;
  - API local `GET http://127.0.0.1:8642/health` retornou `200`/`ok`; `/v1/models` sem bearer retornou `401`, esperado.
- Cron jobs confirmados ativos:
  - `f5a23dd6a1bd` — Hermes/LK daily continuous improvement scan;
  - `edd06fe19397` — Hermes runtime + cron watchdog `no_agent`, última execução OK em `2026-05-14T05:00:10Z`;
  - `e7a61e275c37` — Hermes v0.13 artifacts freshness watchdog `no_agent`, última execução OK em `2026-05-13T12:00:39Z`;
  - `7c688553e293` — LK Daily Sales Brief obrigatório;
  - `953b9055458e` — LK Weekly CEO Review obrigatório;
  - `d4c26da4cd48` — LK GMC Review obrigatório, próximo run `2026-05-14T12:00:00Z`;
  - além dos crons de SEO/CRO e email semanal LK já existentes.
- Logs recentes sanitizados: apenas warnings transitórios de conexão Telegram/API/provider (`Telegram primary connection failed`, `HTTP 503`, `ReadError`/SSL). O helper não reportou alertas e não há evidência de falha persistente de startup.
- Curator v0.12/v0.13: `hermes curator status` confirmou curator habilitado, 2 runs, último há 1 dia, sem mudanças pendentes críticas.
- Skills hub: `hermes skills check` mostrou `impeccable` `up_to_date` e `0 update(s)` disponíveis.

## Release check

Fonte: GitHub releases API de `NousResearch/hermes-agent`.

Última release pública encontrada: `v2026.5.7` — `Hermes Agent v0.13.0 (2026.5.7) — The Tenacity Release`, publicada em 2026-05-07.

Conclusão: não há release pública posterior à versão ativa esperada. Não há plano de update de runtime hoje.

## Diferenças/gaps observados

1. **Gap Hostinger/Docker de ontem fechado**  
   Ontem o helper existia mas falhava em SSH read-only. Hoje ele executou com sucesso, confirmou imagens, estados, versões dos dois containers e cron/logs sanitizados. A rotina diária volta a ter inventário Docker host confiável sem tocar Docker/compose/gateway.

2. **Sem novo release Hermes para atualizar**  
   Como a versão pública mais recente segue sendo v0.13.0, qualquer ação de Docker/runtime hoje seria desnecessária e fora do protocolo.

3. **Warnings transitórios continuam apenas observados**  
   As linhas recentes de log mostram falhas transitórias de conexão/provedor, mas sem alerta do helper, sem queda de container e com API `/health` OK. Não há ação corretiva automática indicada.

## Implementado hoje

- Criado este relatório durável em `reports/hermes-continuous-improvement/2026-05-14.md`.
- Mantido e validado o JSON operacional `reports/hermes-host-docker-observability-2026-05-14.json` como evidência sanitizada do host Docker.
- Atualizada a rotina `areas/operacoes/rotinas/hermes-v013-operacionalizacao.md` com o resumo do run diário de 2026-05-14.
- Atualizado `CHANGELOG.md` com o fato operacional: helper Hostinger/Docker voltou a confirmar imagens/versões dos containers.

## Classificação das oportunidades

### Adopt now

- Manter o helper Hostinger/Docker como primeira etapa do cron diário; hoje ele provou exatamente o que faltava: imagem, estado e versão dos containers no host.
- Manter os watchdogs `no_agent` atuais: runtime/cron e freshness seguem saudáveis/silenciosos quando OK.
- Manter curator habilitado no ciclo semanal; status atual não pede intervenção.
- Manter `lk-growth-ops` como Mission Control sem workers automáticos.

### Teach Lucas manual usage

- `/goal`: usar para missões longas que não devem depender de “seguir” repetido. Exemplo: `/goal Tocar LK OS por 7 dias mantendo Mission Control, relatórios obrigatórios e aprovações separadas de writes/envios`.
- Kanban: usar para status/pendência real da operação LK; falha típica é card sem fonte, sem próximo passo ou misturando aprovação com execução.
- `/background`: usar para auditoria/pesquisa longa enquanto a conversa principal continua; não usar para Docker, deploy, secrets, produção, Shopify writes ou envios externos.
- Curator: não precisa Lucas operar no dia a dia agora; se quiser consultar higiene de skills, comando seguro é `hermes curator status`. Resultado esperado: runs recentes, resumo do último run, skills ativas/stale/archived. Sinal de falha: erro de provider/model ou skill pruning inesperado.

### Approval needed

- Ativar workers Kanban reais, dashboard público/local exposto, troca de imagem/runtime, restart gateway, compose/Traefik/network ou qualquer envio externo LK continuam bloqueados até aprovação explícita.
- Se aparecer nova release Hermes posterior à v0.13, o próximo passo não é deploy: é decision brief com backup de compose/config/env, tag/digest, rollback image e smoke tests usando `/opt/hermes/.venv/bin/python` ou `python3` — nunca `python` puro.

## Próximo passo recomendado

Nenhum runtime change hoje. Prioridade operacional: deixar o helper Hostinger/Docker rodando diariamente como fonte de verdade read-only e acompanhar o primeiro run do `LK GMC Review` hoje às 09:00 BRT (`d4c26da4cd48`), sem criar novos crons recursivamente.
