# Hermes/LK Continuous Improvement — 2026-05-13 02:00 BRT

Contexto: Infra/Hermes + LK/Hermes Brain.  
Ação: read-only + documentação operacional.  
Risco: baixo; nenhuma mudança em Docker, gateway, containers, secrets, bancos, Shopify, Meta/Google, campanhas ou envios externos.

## Verificações read-only executadas

- Data/hora do run: `2026-05-13T05:02:18Z`.
- Helper aprovado de observabilidade Hostinger/Docker executado primeiro: `/opt/data/scripts/hermes_host_docker_observability.py --output /opt/data/hermes_bruno_ingest/hermes-brain/reports/hermes-host-docker-observability-2026-05-13.json`.
- Resultado do helper: falhou em modo read-only com `SSH read-only observability failed rc=255` e erro sanitizado de autenticação SSH (`Permission denied`). Nenhuma ação de Docker/SSH/gateway foi executada.
- Fallback local no container:
  - `/opt/hermes/.venv/bin/hermes --version` retornou `Hermes Agent v0.13.0 (2026.5.7)`;
  - config ativo: `/opt/data/config.yaml`;
  - `hermes config check`: config version `23` OK;
  - `hermes cron status`: scheduler/gateway reportado como rodando neste contexto, PID `7`, próximo run `2026-05-13T05:30:00+00:00`;
  - `hermes cron list --all`: 8 jobs ativos, incluindo o cron diário `f5a23dd6a1bd`, watchdogs `edd06fe19397`/`e7a61e275c37`, LK Daily Sales Brief, LK Weekly CEO Review e LK GMC Review.
- Watchdogs `no_agent` testados manualmente:
  - `hermes_runtime_cron_watchdog.py`: `rc=0`, stdout vazio, stderr vazio;
  - `hermes_artifacts_freshness_watchdog.py`: `rc=0`, stdout vazio, stderr vazio.
- API local: `GET http://127.0.0.1:8642/health` retornou `200` com status `ok`; `/v1/models` sem bearer retornou `401`, esperado.
- Logs locais recentes: gateway reiniciou em 2026-05-12 à noite, reconectou Telegram, API Server e cron ticker; houve um warning transitório de conexão primária Telegram e mensagens inbound posteriores. Não há evidência local de falha persistente de startup neste recorte.
- Kanban `lk-growth-ops`: 8 cards concluídos e 5 cards `ready` sem assignee; nenhum worker/daemon novo ativado.

## Release check

Fonte: GitHub releases API de `NousResearch/hermes-agent`.

Última release pública encontrada: `v2026.5.7` — `Hermes Agent v0.13.0 (2026.5.7) — The Tenacity Release`, publicada em 2026-05-07.

Conclusão: não há release pública posterior à versão ativa esperada. Não há plano de update de runtime hoje.

## Diferenças/gaps observados

1. **Observabilidade Hostinger/Docker ainda não fechou ponta a ponta**  
   O helper aprovado existe e roda sem mutação, mas a conexão SSH read-only falhou com `Permission denied`. Assim, este run ainda não confirmou por host Docker as imagens `hermes-agent-custom:v0.13.0-20260510` nos containers esperados nem leu logs Docker diretamente. O fallback local confirma versão/config/cron/API, mas não substitui inventário Docker host.

2. **Sem novo release Hermes para atualizar**  
   Como a versão pública mais recente segue sendo v0.13.0, qualquer ação de Docker/runtime hoje seria desnecessária e fora do protocolo.

3. **Cards LK prontos seguem intencionalmente sem assignee**  
   O Mission Control está saudável como board/checklist. Workers reais continuam bloqueados por aprovação porque mudariam operação/produção.

## Implementado hoje

- Criado este relatório durável em `reports/hermes-continuous-improvement/2026-05-13.md`.
- Atualizada a rotina `areas/operacoes/rotinas/hermes-v013-operacionalizacao.md` com o resumo do run diário de 2026-05-13.
- Mantido o helper de observabilidade como artefato operacional: o JSON sanitizado de falha ficou salvo em `reports/hermes-host-docker-observability-2026-05-13.json` para auditoria.

## Classificação das oportunidades

### Adopt now

- Manter os watchdogs `no_agent` atuais: ambos seguem silenciosos quando OK.
- Manter `lk-growth-ops` como Mission Control sem workers automáticos.
- Usar o helper Hostinger/Docker no começo de cada run, mas tratar falha SSH como gap operacional, não como autorização para mexer em root/SSH/Docker.

### Teach Lucas manual usage

- `/goal`: usar para missões longas que não devem depender de “seguir” repetido. Exemplo: `/goal Tocar LK OS por 7 dias mantendo Mission Control, relatórios obrigatórios e aprovações separadas de writes/envios`.
- Kanban: usar para status/pendência real da operação LK; falha típica é card sem fonte, sem próximo passo ou misturando aprovação com execução.
- `/background`: usar para auditoria/pesquisa longa enquanto a conversa principal continua; não usar para Docker, deploy, secrets, produção, Shopify writes ou envios externos.

### Approval needed

- Corrigir a autenticação do helper de observabilidade Hostinger/Docker. Benefício: confirmar diariamente imagens, container state e logs Docker sanitizados. Risco/blast radius: toca acesso SSH/infra. Recomendação: preparar correção mínima read-only do acesso usado pelo helper, com rollback claro, sem mudar Docker/compose/gateway.
- Ativar workers Kanban reais, dashboard público/local exposto, troca de imagem/runtime, restart gateway, compose/Traefik/network ou qualquer envio externo LK continuam bloqueados até aprovação explícita.

## Próximo passo recomendado

Prioridade: fechar a autenticação read-only do helper Hostinger/Docker. Hoje ele já é o caminho certo, mas falhou no SSH; sem isso, o cron continua dependente de fallback local e não prova imagem/container no host.
