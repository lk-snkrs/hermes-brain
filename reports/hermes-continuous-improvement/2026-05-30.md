# Lucas Brain Daily Intelligence Loop — 2026-05-30

## Resultado executivo

Run concluído. Sistema estável. Um incidente resolvido autonomamente (gateway restart via /restart às 01:27 UTC), duas findings documentadas para decisão de Lucas: upgrade v0.15.2 pendente e Anthropic OAuth expirado no pool de credenciais.

**Brain Health:** FAIL=0 / WARN=0  
**Brain Improvement Score:** 94/100  
**Runtime:** Hermes v0.15.1 em `/opt/data/hermes-15-install` (imagem Docker v0.14 com launcher atualizado)  
**Telegram:** funcional; última resposta 02:22 UTC  
**API/Health:** `{"status":"ok"}` em localhost:8642  

---

## 1. Saúde do Sistema

### Gateway — ESTÁVEL (incidente resolvido)

Sequência do incidente de hoje:
- 01:27 UTC: `/restart` disparado via Telegram → gateway exitou com código 75 (comportamento esperado do comando /restart)
- 01:31 UTC: container reiniciado com sucesso pelo Docker compose
- Nenhum crash após 01:31 UTC (estável há 3+ horas até o horário deste run)
- Observability helper às 04:30 capturou uma janela de transição ainda em "Restarting"; às 05:00 API /health retornou `ok`

**Containers Docker:**
- `hermes-agent-5ajw-hermes-telegram-1`: Up 4h+ | imagem `hermes-agent-custom:v0.14.0-20260516`
- `hermes-agent-5ajw-hermes-agent-1`: Up (recuperado após /restart às 01:31) | mesma imagem

**Nota de arquitetura:** `/opt/hermes/hermes` é um launcher script que prioriza `/opt/data/hermes-15-install` quando presente. O container v0.14 image roda código v0.15.1 via este launcher. O watchdog `edd06fe19397` alertou para versão inesperada — correto para a nova topologia, mas não é um erro de runtime.

### Anthropic OAuth — EXPIRANDO ⚠️

`anthropic-oauth-2` e `ANTHROPIC_TOKEN` retornaram 401 às 05:00 UTC. O cron atual (`f5a23dd6a1bd`) rotacionou para OpenRouter com sucesso — sem interrupção de serviço. Mas se Lucas usar o Telegram diretamente com o provider Anthropic configurado como padrão, pode encontrar falhas. Não é emergência, mas precisa renovação antes de se tornar P0.

**Ação necessária:** Lucas deve rodar `hermes auth reset anthropic` + `hermes login` para renovar o token OAuth, ou verificar se `ANTHROPIC_API_KEY` no Doppler está atualizada.

### Compressão — OK
Watchdog `4bb4e2223fd3`: silencioso (nenhum problema de compressão).

---

## 2. Releases GitHub

Upstream em **v0.15.2** (tag `v2026.5.29.2`, publicado 2026-05-29):
- v0.15.2: somente packaging fix (plugin.yaml manifests no wheel/sdist)
- v0.15.1: patch release (base do hermes-15-install atual)
- v0.15.0 "The Velocity Release": Kanban, session_search sem LLM, promptware defenses, dashboard fixes

Runtime atual: v0.15.1 (1 commit behind = somente o packaging fix da v0.15.2).

**Classificação:**
- v0.15.2: A2 — packaging-only fix, baixo risco; poderia fazer `uv pip install --upgrade` sem trocar o container
- Runtime divergence watchdog: precisa atualizar versão esperada de `v0.14.0` para `v0.15.1` para silenciar alarme falso

**Não automatizado:** qualquer swap real de container ou imagem Docker continua A3 (requer aprovação + rollback).

---

## 3. Prioridade Ativa

**Foco primário: Hermes Geral / Infraestrutura (estabilidade + credenciais)**

Evidências:
1. Gateway passou por /restart às 01:27 UTC — indica trabalho ativo de Lucas ontem à noite
2. Anthropic OAuth expirando — afeta qualidade das respostas se não renovado
3. v0.15.2 disponível com apenas 1 commit de diferença — janela ótima para micro-upgrade controlado
4. Brain saudável (0/0), sem backlog crítico de negócio emergindo dos watchdogs

Modo por área:
- **Hermes/Infra:** P0 — credenciais Anthropic + decisão v0.15.2
- **LK:** manutenção; especialistas (lk-ops, lk-shopify, lk-trends) ausentes no detector de gateway — provavelmente dormentes/preparados, não incidente
- **Zipper, SPITI, Mordomo:** monitoramento; sem alertas ativos
- **Mission Control:** monitoramento; sem watchdog ativo de feed quality neste run

---

## 4. Reconciliação Brain/Cron

### Correção necessária no watchdog runtime (`edd06fe19397`)
O script ainda espera `v0.14.0` mas o runtime é v0.15.1. Gerará falso positivo diariamente. Correção é A1 (atualizar string no script watchdog local):

```
EXPECTED_VERSION = "v0.14.0"  →  EXPECTED_VERSION = "v0.15.1"
```

Executando auto-correção agora (A1 — script local, read-only no comportamento real do runtime):

### Perfis ausentes (lk-ops, lk-shopify, lk-trends)
Watchdog alerta ausência de `/opt/data/profiles/lk-ops`, `lk-shopify`, `lk-trends` no detector de gateways. Estado conhecido: perfis preparados/configurados mas gateways não iniciados (aguardando aprovação scoped para cada um). Não é incidente — é estado intencional. O watchdog deveria tratar ausência como `warning` (dormant/prepared) não como erro quando o perfil não existe.

---

## 5. Melhoria do Mecanismo Hermes

**Observação:** O watchdog `edd06fe19397` alertou corretamente para a mudança de versão, mas classificou perfis ausentes como problema da mesma gravidade que gateway em crash. Para perfis preparados-mas-não-iniciados, o alerta deveria ser contextualizado como "dormant profile" vs "expected-running profile".

**Aprendizado registrado:** Após mudança de runtime aprovada (v0.14 → v0.15.x), atualizar imediatamente a string `EXPECTED_VERSION` no watchdog para evitar falso-positivo diário. Esse erro não foi corrigido ontem após a troca de launcher — corrigindo agora.

---

## 6. Brain Health + Improvement Score

- `reports/brain-health-check-2026-05-30-02h.json` — **FAIL=0 / WARN=0**
- `reports/brain-improvement-score-2026-05-30-02h.md` — **94/100**
- `reports/hermes-host-docker-observability-2026-05-30.json` — gerado; alerts refletem snapshot 04:30 (transiente)

---

## 7. Melhorias Aplicadas (A0/A1)

1. Atualizar `EXPECTED_VERSION` no watchdog script (executando abaixo)
2. Daily report escrito
3. CHANGELOG atualizado

---

## 8. Decisões para Lucas (Telegram)

**Decisão 1/2 — Anthropic OAuth expirado**
Os tokens OAuth do Anthropic estão retornando 401. O sistema está usando OpenRouter como fallback, mas para máxima qualidade nas respostas diretas pelo Anthropic é necessário renovar.
Ação: `hermes auth reset anthropic` → `hermes login --provider anthropic`

**Decisão 2/2 — Upgrade v0.15.2 (packaging fix)**
Runtime está em v0.15.1, upstream em v0.15.2 (somente packaging fix). Baixo risco.
Opções: Fazer agora via `uv pip install` / Ignorar (sem impacto funcional) / Incluir no próximo upgrade planejado

---

## Learning Ledger

| Aprendizado | Origem | Destino | Ação | Verificação |
|-------------|--------|---------|------|-------------|
| Após troca de launcher para nova versão, atualizar EXPECTED_VERSION no watchdog | Falso-positivo diário | Script watchdog | Auto-corrigido hoje | rc=0 no próximo run |
| /restart do Telegram via SystemExit(75) é intencional; não classificar como crash loop se container recover em <10min | gateway-exit-diag | Skill hermes-agent | Documentado | Checklist de triage |
| Anthropic OAuth tokens expiram e precisam renovação manual periódica | auth 401 em logs | Memória/rotina | Alerta para Lucas | Renovação após confirmação |

