# Hermes Learning Ledger — 2026-06-02

## 1. Watchdog de runtime v0.15.2

- Origem: `Hermes runtime + cron watchdog no_agent` alertou expectativa de versão antiga, enquanto o runtime vivo já estava em v0.15.2.
- Classificação: A1 local/documental/script helper.
- Destino aplicado: scripts runtime em `/opt/data/scripts/` e espelhos em `areas/operacoes/scripts/`.
- Ação feita: `EXPECTED_VERSION_FRAGMENT` e `EXPECTED_VERSION` atualizados para v0.15.2.
- Verificação: `py_compile` OK; watchdog manual `rc=0`/stdout vazio; host observability `alerts: []`.
- Próxima regra: depois de qualquer runtime upgrade, atualizar helpers esperados no mesmo ciclo.

## 2. Relatório gerado não é relatório entregue

- Origem: fechamento 23h + cron LK 16h com falha de entrega externa por `wacli hermes not authenticated`.
- Classificação: skill/procedimento.
- Destino aplicado: skill `lk-report-delivery`.
- Ação feita: adicionada regra explícita separando `report generated` de `report delivered`.
- Verificação: skill patch retornou sucesso.
- Próxima regra: daily loop deve tratar falha de entrega como pendência/alerta, mesmo quando artefatos locais existem.

## 3. wacli hermes não autenticado

- Origem: status read-only das contas wacli e erro de cron LK/Zipper.
- Classificação: P0 operacional que exige fluxo com Lucas presente.
- Destino aplicado: relatório diário e decisão 1/1.
- Ação feita: status read-only verificado; nenhuma autenticação, envio ou leitura de mensagens executada.
- Verificação: `hermes authenticated=False`, `zipper=True`, `spiti=True`, `pessoal=False`.
- Bloqueio: pareamento/reconexão da conta depende de Lucas/WhatsApp e não deve ser iniciado por cron sem presença.
