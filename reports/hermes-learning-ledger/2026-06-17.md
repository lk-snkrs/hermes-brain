# Hermes Learning Ledger — 2026-06-17

## Aprendizado promovido

### 1. Brain Health WARN `decisions_not_linked` deve virar autoheal A1 quando o link é local/documental

- Evidência: preflight encontrou `areas/lk/sub-areas/growth/decisions` com markdowns sem link no `MAPA.md` pai.
- Ação: linkei `decisions/` no MAPA de LK Growth com ressalva explícita de que indexar não ativa write, rotina ou cron.
- Verificação: Brain Health pós-run retornou `FAIL=0/WARN=0`.
- Escopo: local/documental, sem writes externos, sem runtime, sem Shopify/Tiny/GMC/WhatsApp/email.

### 2. Timeout de cron com escopo WhatsApp/external-write não deve ser tratado como falha genérica de Hermes

- Evidência: `LC Mordomo OS whatsapp_context_engine` teve `last_status=error` por timeout de 120s; `deliver=local`, `last_delivery_error=null`.
- Classificação: alerta novo localizado no perfil Mordomo, não incidente de gateway/Docker.
- Próximo seguro: investigar módulo/output e preparar correção limitada. Mudanças que alterem comportamento externo, restart, schedule ou runtime exigem aprovação escopada.

### 3. Release watch sem tag nova deve evitar re-sumarização repetida

- Evidência: upstream segue em `v2026.6.5`; `p2_release_adoption_planner.action=skip_repeated_release_review`.
- Ação: mantida classificação como já adotado/sem nova matriz hoje.

## Estado de aprendizagem

- Brain Health: limpo pós-autoheal.
- Memory OS/Reminder OS/gateway watchdog: sem regressão crítica no preflight.
- Dívida contínua: `skill_quality_audit` ainda aponta 78 skills com risco de tamanho/staleness; resolver por ondas específicas, não dentro de um patch cego.

values_printed=false
