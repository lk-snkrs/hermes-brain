# Hermes Learning Ledger — 2026-06-13

## Aprendizados promovidos

1. `spiti-atendimento` é um gateway Telegram gerenciado e deve constar no runtime watchdog como `REQUIRED_GATEWAY_HOMES` quando o all-gateway watchdog já o gerencia e a verificação live confirma API/webhook off.
2. Para jobs `no_agent`, alerta de conteúdo/endpoint deve sair por stdout com `rc=0`; `rc!=0` fica reservado para defeito de execução do script.
3. Mirrors do Brain para scripts operacionais precisam ser alinhados com o script ativo no mesmo ciclo, especialmente quando o ativo já foi aprovado/gerenciado e o mirror está defasado.
4. Correções de watchdog/cron devem terminar com `py_compile`, execução manual silent-OK quando aplicável, receipt via Memory OS e scan/health pós-run.

## Regras operacionais reforçadas

- Autonomia: correções deste ciclo foram A1 local/read-only/documental/script-safe, com verificação objetiva antes de finalização.
- Nenhum Docker/VPS/Traefik/gateway/container/restart foi executado.
- Nenhum write externo, Shopify/Tiny/GMC/Klaviyo/Meta/WhatsApp/e-mail/database foi executado.
- Nenhum secret foi impresso; `values_printed=false`.
- Estoque/pronta entrega/grade/ruptura/SKU continua roteado obrigatoriamente para `lk-stock`; este ciclo não consultou nem alterou estoque.

## Evidência

- Runtime watchdog manual: silent-OK após adicionar `spiti-atendimento` ao allowlist requerido.
- All-gateway watchdog manual: silent-OK após alinhar mirror do Brain.
- LK AI/GEO monitor: `rc=0`, `Veredito: OK`, `Falhas: 0` em execução direta pós-fix.
- Receipt: `areas/operacoes/receipts/daily-intelligence-autoheal-20260613.md`.

values_printed=false
