# Hermes Learning Ledger — 2026-06-12

## Padrão aprendido
- Quando um perfil especialista aprovado vira `managed` no `hermes_all_gateway_watchdog.py`, o `hermes_runtime_cron_watchdog.py` precisa reconhecer o mesmo `HERMES_HOME` como REQUIRED ou OPTIONAL_DORMANT. Caso contrário, o próximo no_agent vira falso positivo recorrente.
- O caso de hoje foi `lk-finance`: o gateway já estava ativo/gerenciado e o Doppler presence-only estava ok, mas o runtime watchdog ainda não conhecia o perfil.

## Autonomia / A1 aplicado
- Ação classificada como A1 local/script parity: patch em watchdog ativo + cópia Brain, sem reiniciar processos e sem alterar cron real.
- Verificação exigida para repetir este padrão: `py_compile`, execução manual silent-OK, preflight sem alertas ativos, receipt Memory OS e Brain Health.

## O que foi aplicado neste run
- Corrigido `REQUIRED_GATEWAY_HOMES` para incluir `/opt/data/profiles/lk-finance`.
- Receipt criado via writer: `reports/governance/receipts/daily-loop-lk-finance-watchdog-reconciliation-20260612.md`.
- Release review pulado corretamente por ausência de tag nova.
- A3/A4 mantidos bloqueados; nenhuma mutação runtime/externa executada.

## Próximo safe action quando houver janela
- Continuar a redução de `skill_quality_drift` em ondas pequenas: primeiro referências quebradas de skills de roteamento/governança, depois split de skills oversized em `references/`.

values_printed=false
