# Hermes Learning Ledger — 2026-06-15

## Padrão aprendido
Quando o `nightly_self_improvement_regression` aponta `reminder_health_not_pass` + `reminder_ingress_open_needed`, a causa pode ser pequena e local: status não-canônico no ledger JSONL ou loop histórico superseded ainda ativo. O tratamento correto é reparar o ledger A1, validar schema/ingress/health, e manter os loops reais ativos com owner e gatilho.

## Autonomia / A0 / A1
A correção foi A1 local/documental: editar `areas/operacoes/reminder-os/reminders.jsonl`, sem writes externos, sem cron mutation, sem Docker/VPS/gateway/restart, sem secrets e sem execução de tarefas de negócio. A decisão de validar estoque Nike Mind permanece com `lk-stock`; outros perfis só podem usar linguagem segura até retorno do owner.

## Verificação
- Reminder OS health gate: PASS.
- Ingress audit: `open_needed_count=0`.
- Ledger JSONL: 27 linhas parseáveis.
- Receipt local criado via Memory OS writer.

## Próxima aplicação
Se este padrão reaparecer, primeiro rodar `reminder_os_report.py --json --no-kanban`, identificar a linha de schema ou handoff descoberto, corrigir localmente só se houver evidência segura, e registrar receipt. Não criar cron novo nem mexer em runtime para esse tipo de gap.

values_printed=false
