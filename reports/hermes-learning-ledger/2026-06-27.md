# Hermes Learning Ledger — 2026-06-27

## Aprendizados promovidos hoje

1. **Branch/switch de Brain pode remover módulos que crons de perfis importam do Brain.**
   - Evidência: `LK Stock Gate B daily freshness reconcile` falhou por `ModuleNotFoundError: stock_local_db` após switch/restauração de Brain.
   - Ação aplicada: restauração A1 dos módulos locais do Gate B a partir de backup verificado, com `py_compile` e smoke em DB temporário.
   - Superfície afetada: `areas/lk/sub-areas/stock/scripts/` + cron do perfil `lk-stock`.
   - Próxima proteção desejável: validator local para detectar imports de scripts de perfil que apontam para módulos ausentes no Brain.

2. **Alertas de cron LK precisam separar geração, entrega externa e fonte de verdade.**
   - `LK 09h external delivery` falhou antes de entrega por payload incompleto; isso não autoriza reenvio nem mudança externa.
   - `LK Stock Tiny full sync` falhou em read-only/local por DB/pointer vazio; isso não é Tiny indisponível nem autorização para write.

3. **Doppler map drift não é ausência de credencial.**
   - `lk-shopify` segue com `LK_GOOGLE_PLACE_ID` faltante no mapa esperado.
   - Regra preservada: reportar presença/status/nome apenas, `values_printed=false`, e validar se é segredo obrigatório ou mapa esperado obsoleto antes de afirmar integração ausente.

## Auto-fix aplicado
- Receipt: `areas/lk/sub-areas/stock/receipts/lk-stock-gate-b-daily-module-restore-20260627.md`.
- Escopo: local/read-only/script-safe; sem runtime restart, sem writes externos, sem secrets.

## Dívida controlada
- 83 skills com sinal de oversized/staleness permanecem dívida controlada. Próxima onda A1 deve focar poucas skills de alto uso, não tentar reescrever a biblioteca inteira.
- `LK Stock Tiny full sync` precisa investigação separada do pointer/DB.

values_printed=false
