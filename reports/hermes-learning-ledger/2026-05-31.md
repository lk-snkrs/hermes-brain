# Hermes Learning Ledger — 2026-05-31

## Aprendizados promovidos

| Aprendizado | Origem | Destino aplicado | Ação feita | Verificação | Bloqueio / próxima regra |
|---|---|---|---|---|---|
| O profile `[LC] Claude Cli` é profile local/CLI de brainstorm, não gateway Telegram esperado por padrão. | Alerta do `Hermes runtime + cron watchdog no_agent` às 04:30 UTC + receipt `lc-claude-cli-criado-2026-05-30.md`. | `/opt/data/scripts/hermes_all_gateway_watchdog.py` | Removido `lc-claude-cli` da lista `EXPECTED` para impedir auto-start futuro pelo watchdog global. | `python3 -m py_compile /opt/data/scripts/hermes_all_gateway_watchdog.py` OK; execução manual do watchdog global rc=0/stdout vazio. | O processo já iniciado continua vivo; parar gateway é mutação runtime e fica como decisão/ação aprovada, não auto-fix silencioso. |
| Host observability ainda alerta versão esperada porque o helper compara contra baseline antiga, apesar do runtime vivo estar em `v0.15.1`. | `reports/hermes-host-docker-observability-2026-05-31.json`. | Relatório diário / backlog de watchdogs. | Classificado como drift de expectativa/observabilidade, não incidente runtime, pois containers estão `running` e `/health`/cron estão vivos. | Helper deve ser reconciliado para `v0.15.1` em ciclo seguro; sem Docker/restart. | Não reportar como queda de produção quando versão real está saudável e a diferença é expectativa stale. |
| `v0.15.2` segue sendo release upstream mais recente e é packaging fix. | GitHub Releases API `https://api.github.com/repos/NousResearch/hermes-agent/releases?per_page=10`. | Relatório diário / changelog. | Sem runtime swap; classificado como oportunidade A3 baixa, não incidente. | Upgrade só com pacote/rollback e aprovação. | Não atualizar produção automaticamente em cron. |

## Guardrail aplicado

Nenhuma ação externa, Docker/VPS/gateway stop/restart, cron registry write, Shopify/Tiny/GMC/Meta/Klaviyo/Supabase, WhatsApp/e-mail ou secret foi executado. A única correção aplicada foi local/script A1 para impedir nova autoativação indevida do profile `lc-claude-cli` pelo watchdog global.
