# Hermes Learning Ledger — 2026-06-09

## Padrão aprendido
Quando um especialista LK recém-aprovado fica ativo/gerenciado, o watchdog global e o runtime watchdog precisam concordar no mesmo ciclo. Se o all-gateway manager já lista o perfil como `managed`, mas o runtime watchdog o chama de HERMES_HOME inesperado, isso é drift de governança local, não incidente de Docker/gateway.

## Autonomia aplicada
A1: patch local testável em script de watchdog + cópia-fonte no Brain, sem restart, sem cron mutation, sem secrets e sem produção.

## Regra operacional
Para LK Stock, estoque/pronta entrega/grade/ruptura/SKU continua roteado ao perfil `lk-stock`; Tiny é verdade de estoque e Shopify é somente superfície/evento.

## Verificação
`py_compile` passou; execução manual do runtime watchdog retornou silent-OK (`rc=0`, stdout vazio). values_printed=false.
