# Hermes Learning Ledger — 2026-06-29

## Aprendizados operacionais

1. **Release probe não pode depender de um único CLI.** O `gh api` retornou `rc=4`; o caminho seguro é recuperar a checagem via GitHub Releases API read-only, atualizar `reports/hermes-release-watch/latest.json` e reportar `source` + `values_printed=false`.
2. **Cron non-ok já autohealed não deve virar pânico repetido.** O erro `token()` do `LK Shopify Sales OS nightly full reconcile read-only` já tem receipt A1 de 2026-06-28. Até o próximo run aprovado, classificar como `atenção controlada / aguardando readback`, não como novo incidente de runtime.
3. **Dívida de skills/tracking continua sendo a fraqueza sistêmica.** Brain Health passou, mas o score de melhoria mostra `tracking=76` e `skills=78`; priorizar ondas pequenas e verificáveis, não reescrita ampla de skill oversized.
4. **Honcho deve ser descrito com precisão.** Neste run ele está `configured`, `active`, `functioning` e governado; foi útil só como confirmação de governança de memória, não como fonte de decisão operacional.

## Propagação / próximos usos

- Próximos relatórios devem manter a distinção entre: alerta novo, alerta recorrente, alerta já autohealed aguardando run, e decisão deferida por Lucas.
- Não reabrir a revisão `LK_GOOGLE_PLACE_ID` sem Lucas reabrir o item ou sem packet escopado aceito; decisão de 2026-06-28 foi deferir.

values_printed=false
