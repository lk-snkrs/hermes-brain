# Hermes Learning Ledger — 2026-06-30

values_printed=false

## Padrão aprendido
Quando o all-gateway watchdog alerta `WEBHOOK_SECRET` herdado em perfil secundário, tratar como gap de ativação/runtime, não como secret ausente nem como falha de documentação. Se o processo já está vivo e conectado, a correção real exige restart/replace do gateway secundário e portanto aprovação escopada.

## Autonomia A0/A1 aplicada
- Confirmar por `/proc` apenas booleans/nomes de variáveis é read-only e seguro.
- Criar approval packet local com escopo/rollback/readback é A1 e reduz risco de repetir alerta genérico.
- Verificar releases via broker GitHub read-only é A0 e evita reportar erro transitório do `gh api` do preflight como atualização pendente.

## Limite de autonomia
Não matar/reiniciar processos de gateway dentro do Daily Intelligence. Mesmo para perfis secundários, isso muda runtime Telegram e deve ocorrer fora deste cron com aprovação escopada, rollback e readback.

## Próximo uso
Se Lucas aprovar a correção, executar somente o pacote `reports/governance/gateway-webhook-secret-remediation-2026-06-30.md` e depois verificar novos PIDs com `WEBHOOK_SECRET` ausente, API/webhook off e Telegram connected. Não tocar Main/Docker/VPS/Traefik.
