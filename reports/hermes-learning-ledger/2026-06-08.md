# Hermes Learning Ledger — 2026-06-08

## 1. LK Content profile virou runtime esperado

- Origem: watchdog runtime/cron `edd06fe19397` alertou `/opt/data/profiles/lk-content` como HERMES_HOME inesperado após Gate 10 LK Content aprovado.
- Padrão aprendido: quando Lucas aprova ativação/gestão de um profile especialista, a lista `REQUIRED_GATEWAY_HOMES` precisa ser reconciliada com o manager global no mesmo ciclo.
- Ação aplicada: `/opt/data/scripts/hermes_runtime_cron_watchdog.py` e espelho Brain `areas/operacoes/scripts/hermes_runtime_cron_watchdog.py` passaram a incluir `/opt/data/profiles/lk-content` como required.
- Verificação: `py_compile` OK; runtime watchdog manual `rc=0` com stdout vazio; all-gateway watchdog manual `rc=0` com stdout vazio.
- Autonomia: A1 local/read-only watchdog alignment; sem Docker/gateway/cron registry/profile config mutation.
- Próxima regra: todo novo profile gerenciado deve aparecer em `hermes_all_gateway_watchdog.py` e no runtime watchdog, ou em `OPTIONAL_DORMANT_GATEWAY_HOMES` quando preparado mas não ativo.

## 2. Handoff de governança precisa declarar sanitização explicitamente

- Origem: `hermes_nightly_governance_artifacts_check.py --mode check` apontou ausência de `01h handoff sanitization.secrets_redacted is not true`.
- Padrão aprendido: `values_printed=false` não substitui o campo explícito `secrets_redacted=true` para validação determinística.
- Ação aplicada: `reports/daily-consolidation/2026-06-08-handoff.json` e `reports/daily-consolidation/latest-handoff.json` receberam `secrets_redacted: true` e `values_printed: false`.
- Verificação: governance checker rerodado no fechamento deste run com `rc=0` e stdout vazio.
- Autonomia: A1 documental/validador; sem secrets expostos.
- Próxima regra: handoffs gerados pelo 01h devem manter objeto `sanitization` com campos explícitos para redaction/value-printing.

## 3. LK Content Gate 10 não expande permissões

- Origem: Fechamento 01h e receipts LK Content registraram aprovação de 3 crons planning/read-only no profile `lk-content`.
- Padrão aprendido: aprovação Gate 10 cobre crons próprios e outputs planning/read-only; não cobre Telegram group config, gateway restart, writes externos ou automações amplas.
- Ação aplicada: report diário registrou exclusões e manteve pendências separadas.
- Verificação: receipts lidos; watchdogs voltaram a silent-OK após alinhamento.
- Autonomia: A0/A1 documentação e monitoramento; A3/A4 continuam bloqueados.
- Próxima regra: primeiro output real dos crons LK Content deve ser observado para UX limpa e sem wrapper técnico.
