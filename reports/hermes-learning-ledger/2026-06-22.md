# Hermes Learning Ledger — 2026-06-22

## Aprendizado operacional do dia

### 1. Watchdog de versão precisa acompanhar upgrade real no mesmo ciclo
- Sinal: runtime local observado em `Hermes Agent v0.17.0 (2026.6.19)`, mas `hermes_runtime_cron_watchdog.py` ainda esperava `v0.16.0`.
- Causa: helper local/Brain mirror não acompanhou a atualização de runtime já efetivada.
- Correção A1 aplicada: atualização dos dois helpers ativos e dois espelhos Brain para `v0.17.0` / `2026.6.19`.
- Verificação: `py_compile` OK; runtime watchdog manual rc=0 e stdout vazio.
- Limite: nenhum Docker/VPS/gateway/restart/deploy/cron mutation.

### 2. Host observability timeout é gap de observabilidade, não prova de runtime quebrado
- Sinal: helper Host/Docker read-only terminou com timeout SSH.
- Classificação: atenção controlada; usar runtime local + watchdogs + cron registry como evidência do ciclo, sem inventar falha de Docker.
- Próximo passo seguro: reexecutar helper read-only em ciclo futuro se necessário; qualquer mutation de host/gateway continua A3/A4.

### 3. Skill-quality backlog continua como dívida sistêmica
- Sinal: 79 skills flagged por oversized/stale markers.
- Ação hoje: não mexer em massa dentro do cron; registrar como recorrente controlado.
- Próximo A1 recomendado: quebrar uma skill grande por vez em `references/` com readback e validação, começando por skills que afetam cron/Hermes runtime.

## Superfícies atualizadas
- `/opt/data/scripts/hermes_runtime_cron_watchdog.py`
- `/opt/data/scripts/hermes_host_docker_observability.py`
- `areas/operacoes/scripts/hermes_runtime_cron_watchdog.py`
- `areas/operacoes/scripts/hermes_host_docker_observability.py`
- `receipts/operacoes/hermes-runtime-watchdog-version-alignment-2026-06-22.md`

## Guardrails
- Writes externos: nenhum.
- Secrets: nenhum valor impresso; `values_printed=false`.
- Runtime/prod: nenhuma mutação de Docker/VPS/gateway/container/Traefik/cron real.

values_printed=false
