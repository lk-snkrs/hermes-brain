# Hermes Learning Ledger — 2026-06-04

## 1. Watchdog global deve acompanhar especialistas recém-ativados

- **Origem:** `Hermes runtime + cron watchdog no_agent` alertou `HERMES_HOME inesperado: /opt/data/profiles/lk-collection-optimizer` às 04:30 UTC.
- **Padrão aprendido:** quando Lucas aprova e ativa um especialista com profile dedicado, o watchdog global precisa ser reconciliado no mesmo ciclo; caso contrário, um estado correto vira falso positivo recorrente.
- **Ação aplicada:** adicionado `/opt/data/profiles/lk-collection-optimizer` em `REQUIRED_GATEWAY_HOMES` no script ativo `/opt/data/scripts/hermes_runtime_cron_watchdog.py` e no espelho Brain `areas/operacoes/scripts/hermes_runtime_cron_watchdog.py`.
- **Verificação:** `py_compile` OK; execução manual do watchdog voltou a contrato silent-OK em modo cron/no_agent; processo vivo verificado com API/webhook desligados.
- **Autonomia:** A1 local/read-only/documental + ajuste de script watchdog; sem Docker, gateway restart, cron real, produção, secrets ou write externo.
- **Próxima regra:** todo specialist profile ativado com aprovação escopada deve entrar no mapa de watchdogs como `REQUIRED` se deve estar rodando, ou `OPTIONAL_DORMANT` se apenas preparado.

## 2. Dashboard público continua bloqueio de segurança para cockpit operacional

- **Origem:** handoff 01h registrou risco alto no dashboard público Hermes.
- **Padrão aprendido:** dashboard público vivo não é automaticamente superfície segura para Mission Control/plugin operacional.
- **Ação aplicada:** mantido como decisão A3/A4; nenhuma mitigação infra/auth executada neste run.
- **Verificação:** host observability sem alertas de runtime, mas pendência permanece por receipt específico.
- **Autonomia:** bloqueado até approval packet com backup/rollback.

## 3. Geração local de relatório ≠ entrega externa

- **Origem:** cron list/host observability ainda mostra falhas de entrega WhatsApp LK/Zipper por `wacli hermes` não autenticado.
- **Padrão aprendido:** relatórios podem estar gerados e saudáveis localmente enquanto o canal externo falha; a síntese deve separar geração, entrega e autenticação do canal.
- **Ação aplicada:** registrado no relatório diário como pendência de entrega, sem tentativa de pairing/envio.
- **Verificação:** host observability salvo em `reports/hermes-host-docker-observability-2026-06-04.json`.
- **Autonomia:** pairing/envio WhatsApp permanece A4; requer Lucas presente/escopo explícito.
