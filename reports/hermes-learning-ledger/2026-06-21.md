# Hermes Learning Ledger — 2026-06-21

values_printed=false

## Padrão aprendido

Alertas recorrentes não devem ser repetidos como se fossem novos. Quando o `alert_dedupe` mostra recorrência e já existe approval packet local, o Daily Intelligence Loop deve apontar para o packet, preservar o próximo passo e evitar novas mudanças locais redundantes.

## Evidência

- `nightly_self_improvement_regression_attention` recorrente, visto 3 vezes.
- Causa: Memory OS provider guard contra `memory.provider: honcho`.
- Packet já criado: `areas/operacoes/decision-inbox/memory-provider-honcho-policy-divergence-20260620.md`.
- Lucas já aprovou `Fazer` para preparar o packet; ainda falta escolher A/B.

## Autonomia / A0-A4

- A0/A1 executado: read-only audit, classificação do alerta, geração dos artifacts diários e verificação local.
- A2 pendente: Lucas escolher uma das opções do packet de governança.
- A3/A4 bloqueado: alterar `/opt/data/config.yaml`, runtime de memory provider, gateway, Docker/VPS, cron delivery/schedule ou secrets.

## Próxima ação segura

Manter o alerta como `Atenção controlada` até Lucas responder:

- `Aprovar A` — aceitar Honcho governado e ajustar política/watchdogs locais;
- `Aprovar B` — manter política antiga e preparar rollback/desativação em pacote separado.

Sem nova ação local necessária hoje além de manter os artifacts consistentes.
